use_frameworks!

target 'RichPushSample' do
  pod 'SwrveSDK', :path => '../../..'
  pod 'SwrveSDKCommon', :path => '../../..'

  # use remote podfile by commenting out above and uncommenting below
  #pod 'SwrveSDK'
end

target 'RichPushSampleServiceExtension' do
  pod 'SwrveSDKCommon', :path => '../../..'

  #use remote podfile by commenting out above and uncommenting below
  #pod 'SwrveSDKCommon'
end
